pipeline {
  agent any
  stages {
    stage('Install npm') {
      steps {
        sh 'node --version'
        sh 'npm --version'
      }
    }
    stage('Test') {
      steps {
        sh 'npm --version'
      }
    }
  }
}